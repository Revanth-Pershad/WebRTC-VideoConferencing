<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Video Conference</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css">
  <script src="https://cdn.jsdelivr.net/npm/nanoid@3.1.20/dist/nanoid.umd.js"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center">

  <div class="p-6 bg-white shadow-md rounded-lg w-full max-w-md">
    <h1 class="text-2xl font-semibold mb-4 text-center text-gray-800">Video Conference</h1>

    <div class="mb-4">
      <button
        id="createRoomBtn"
        class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300 w-full disabled:bg-blue-200"
        onclick="createNewRoom()"
      >
        Create New Room
      </button>
    </div>

    <div class="mb-4">
      <input
        type="text"
        id="roomUrl"
        placeholder="Enter Room URL"
        class="px-4 py-2 border rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-300 mb-2"
      />
      <button
        id="joinRoomBtn"
        class="px-4 py-2 bg-green-500 text-white rounded hover:bg-green-600 transition duration-300 w-full disabled:bg-green-200"
        onclick="joinRoom()"
      >
        Join with Room Url
      </button>
    </div>

    <p class="text-center text-sm text-gray-500 mt-4">
      Welcome to the simplest way to connect. Enter a username, then create a new room to start a meeting or join an existing one.
    </p>
  </div>

  <script>
    function createNewRoom() {
        window.location.href = '/getRoom';
    }

    function joinRoom() {
      const roomUrl = document.querySelector('#roomUrl').value.trim();

      if (!roomUrl) return;

      window.location.href = roomUrl;
    }
  </script>

</body>
</html>
